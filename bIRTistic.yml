name: birtistic
channels:
  - conda-forge
  - defaults
dependencies:
  # R 4.5.2
  - r-base=4.5.2
  
  # CmdStan 2.37
  - cmdstan=2.37
  
  # Build tools (required for CmdStan and package compilation)
  - make
  - cxx-compiler
